#include <stdio.h>
/**
 * main: Entry point
 *
 * Return: Always 0 (Success)
 *
 * description: The program will print a string using printf function
 */
int main(void)
{
printf("\"with proper grammar, but the outcome is a piece of art,");
return (0);
}
